<div class="fotorama">
  <% options = { full_path: false, sorted: true } %>
  <% each_file("data#{get_full_url(directory)}", options) do |name| %>
  <a href="<%= digest_url("#{directory}/#{name}") %>"></a>
  <% end %>
</div>

<div class="book-thumb">
  <a href="<%= "/books/#{book.id}/" %>">
    <img class="cover"
         src="<%= book_cover_url(book.id, :medium) %>"/>
    <div class="title">
      <%= book.title %>
    </div>
  </a>
  <% if not book.authors.nil? %>
    <div class="author">
      <%= book.authors %>
    </div>
  <% end %>
</div>

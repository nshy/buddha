<div class="library">
  <div class="title">
    <h1> Библиотека </h1>
  </div>
  <% @library.section.each do |section| %>
    <div class="section">
      <h2> <%= section.name %> </h2>
        <% section.category.each do |category| %>
          <div> <h3> <%= category_link(@categories, category) %> </h3> </div>
        <% end %>
    </div>
  <% end %>
</div>
